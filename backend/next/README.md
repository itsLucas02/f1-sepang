# Historical static-demo staging area

The Supabase runtime is active at the normal Next.js paths:

- `/proxy.ts`
- `/app/auth/callback/route.ts`
- `/app/api/predictions/route.ts`

`next.config.ts` no longer uses static export. Configure the production environment variables documented in `.env.example` before deployment.
