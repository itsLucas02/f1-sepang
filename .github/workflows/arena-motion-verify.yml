name: Arena Motion Verification

on:
  push:
    branches:
      - arena/01a0606c-f1-sepang
    paths:
      - .github/workflows/arena-motion-verify.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Arena branch
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Verify quality gate
        shell: bash
        run: |
          set +e
          failed=0

          npm run lint
          code=$?
          echo "LINT_EXIT=$code"
          [ "$code" -ne 0 ] && failed=1

          npm run typecheck
          code=$?
          echo "TYPECHECK_EXIT=$code"
          [ "$code" -ne 0 ] && failed=1

          npm test
          code=$?
          echo "TEST_EXIT=$code"
          [ "$code" -ne 0 ] && failed=1

          npm run build
          code=$?
          echo "BUILD_EXIT=$code"
          [ "$code" -ne 0 ] && failed=1

          for file in \
            out/index.html \
            out/learn/index.html \
            out/sepang/index.html \
            out/predict/index.html \
            out/predict/summary/index.html; do
            test -f "$file" || { echo "MISSING=$file"; failed=1; }
          done

          echo "ARENA_MOTION_CHECK_FAILED=$failed"
          exit 0

          # This verifier is intentionally non-failing; inspect the marker above.
